Use this bundled workflow to resolve Makestar event codes with verified read-only CLI commands.

- Start from the event code and resolve to product event detail before inferring product contents.
- Use the CLI's product content and SKU commands instead of inventing request shapes.
- Keep the answer grounded in visible product content, concrete SKU, inventory, and price fields.
- Treat historical case notes as source-maintainer context only; installed bundles should be usable without them.
