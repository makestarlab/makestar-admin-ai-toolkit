For Makestar operator lookup questions, use the smallest verified read-only script command first.

Answer shape:
- visible fields first
- ids/keys second
- server-side total count first when available
- raw(label) for known enum/grade values
