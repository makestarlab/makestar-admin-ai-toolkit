<!-- managed:supported_cli_requirement -->
> Requires `makestar-admin` >=0.2.10. Run `makestar-admin --version` before CLI-dependent actions.
> Cowork/sandboxed Linux agents should resolve/install with the generated package helper (`cowork/cowork-cli-bootstrap.sh` in AI Toolkit exports, or `references/cowork-cli-bootstrap.sh` when bundled) and use its verification evidence before CLI actions.
> Host shells keep Homebrew, winget/MSI, or public release archive setup.
<!-- /managed:supported_cli_requirement -->

Resolve Makestar event code to option/content/SKU/inventory/price using the verified script-first workflow.

Start from the event code, use read-only `makestar-admin` commands, and keep the answer grounded in visible product content, concrete SKU, inventory, and price fields.
